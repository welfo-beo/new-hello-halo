node_modules
dist
build
out

# TypeScript build cache
*.tsbuildinfo
.halo

# Built-in digital humans bundle (build artifact, generated by
# scripts/sync-builtin-apps.mjs from the SSOT declared in product.json).
# Each product variant points at its own SSOT so this directory is per-build,
# never checked in.
resources/builtin-apps/

# Portable Git archive for Windows builds (downloaded by scripts/prepare-binaries.mjs)
resources/git-bash/

# Release notes temp files
.release-notes-current.md
.release-notes-summary.md

# Local environment variables (secrets)
.env.local

# E2E test results
tests/e2e/results/

app.log
# Local docs
local_docs/

# Local plugins
halo-local/
product.json
product.local.json

# Active product variant pointer (per-machine, managed by `npm run product`)
.product-source

# Local debug scripts
vscode-debug/

# Local deploy scripts
scripts/deploy_local_*.sh

# Backup files
*.backup
*.bak
*.patch.back
*copy.*

# IDE share/export artifacts
.trae-html-share-packages/

# Website (landing page, managed separately)
website/

# Capacitor mobile build output
dist-mobile/

# Android build artifacts (Gradle caches, APKs)
android/app/src/main/assets/public/*
android/app/build/
android/.gradle/
android/build/
android/capacitor-cordova-android-plugins/build/
android/local.properties
*.apk
*.keystore

# OS files
.DS_Store
RELEASE_NOTES.md
# temp

# Internal digital human skills (wekm)
.claude/skills/wekm-*

# Halo Agent SDK (managed in separate private repo)
src/sdk/
/temp/
# Stray KB wiki artifacts (real wikis live under ~/.halo, never in-repo)
/wiki/
# Personal marketing/intro drafts (not product code)
/intro-doc/

# Local git worktrees created for isolated work
.claude/worktrees/
